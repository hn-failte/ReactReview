import React from 'react'
import { useDrag } from 'react-dnd'
import { useDrop } from 'react-dnd'

// useDrop()

export default () => {
    // const [collectionProp, drag] = useDrag({item: {type: 'drag'}})
    // const [collectionProp, drag] = useDrag({item: {type: Symbol()}})
    // useDrop({item: {type: Symbol()}})
    return <div>
    {/* return <div ref={drag}> */}
        dnd
    </div>
}
